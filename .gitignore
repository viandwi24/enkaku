node_modules/
dist/
*.db
*.db-journal
*.db-wal
*.db-shm
.DS_Store
.env
.env.*
# ...but the templates are the documentation, so they must be committed.
!.env.example
!packages/studio/.env.example

# next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# rust / tauri
target/
apps/desktop/src-tauri/binaries/
apps/desktop/src-tauri/gen/schemas/

# data dev
.dev-data/
# Stray per-test data dirs (a subagent left one under packages/core once).
**/.dev-data-*/
.dev-cloud/
.dev-agent/
.agent-data/

# release build
*.gen.ts
release/
# Built by scripts/build-packs.ts from plugins/*/src — an artifact, never a source.
packages/core/packs/


refs/
