{
  "name": "@enkaku/plugin-proxy-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Proxy manager — a catalogue of proxy records, and a screen to keep it with. No proxy is ever contacted",
  "enkaku": {
    "plugin": "proxy-manager",
    "entry": "src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "publish:farm": "bun run ../../packages/sdk/src/cli/index.ts publish src/index.ts"
  },
  "dependencies": {
    "@enkaku/protocol": "workspace:*",
    "@enkaku/sdk": "workspace:*",
    "zod": "^4.0.0"
  }
}
