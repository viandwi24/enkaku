{
  "name": "@enkaku/plugin-networking",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "Networking — leak and egress checks driven through a real browser on the device",
  "enkaku": {
    "plugin": "networking",
    "entry": "src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "publish:farm": "bun run ../../packages/sdk/src/cli/index.ts publish src/index.ts"
  },
  "dependencies": {
    "@enkaku/protocol": "workspace:*",
    "@enkaku/sdk": "workspace:*",
    "zod": "^4.0.0"
  }
}
